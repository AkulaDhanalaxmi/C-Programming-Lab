#include<stdio.h>
int main(){
	int side,area;
	printf("Enter the value of side of the square:");
	scanf("%d",&side);
	area=side*side;
	printf("The area of the square is %d.",area);
	return 0;
}
